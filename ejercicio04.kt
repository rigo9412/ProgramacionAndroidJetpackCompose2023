// Elaborar una función que calcule el total a partir de un descuento dado.

fun main() {

    val totalSD = 12
    val desc = 50
    val totalCD: Double
    
    totalCD = (desc/100.0)*totalSD
    println(totalCD)

    
}